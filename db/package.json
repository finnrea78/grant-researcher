{
  "name": "@grant-researcher/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "_exports_note": ".ts exports are intentional: data-pipeline runs via node --import=tsx (handles .ts natively); Next.js uses webpack which resolves .ts workspace imports natively. Run 'npm run build' to produce compiled .js output if needed for plain-node consumption.",
  "scripts": {
    "build": "tsc",
    "db:push": "supabase db push",
    "db:status": "supabase migration list",
    "db:diff": "supabase db diff"
  },
  "devDependencies": {
    "supabase": "^2.0.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.49.0"
  }
}
